<div class="masthead">
  <%= render "theme_toggle" %>
  <header class="site-header">
    <h1 class="site-name">
      <a href="/"><%= metadata.title %></a>
    </h1>
  </header>
  <%= render "navigation", metadata: metadata %>
</div>
